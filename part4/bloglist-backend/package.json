{
  "name": "bloglist-backend",
  "version": "1.0.0",
  "description": "fullstackopen part4 bloglist-backend",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "start:test": "cross-env NODE_ENV=test nodemon index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "jest --verbose",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "author": "mkibar",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.2.4"
  },
  "devDependencies": {
    "eslint": "^8.10.0",
    "jest": "^27.5.1",
    "nodemon": "^2.0.15"
  }
}
